require('dotenv').config();
const bcrypt=require('bcryptjs'); const {Pool}=require('pg'); const pool=new Pool({connectionString:process.env.DATABASE_URL});
(async()=>{const username=process.argv[2]||'admin'; const password=process.argv[3]; const name=process.argv[4]||'Master Administrator'; if(!password){console.error('Gunakan: node create-admin.js admin PasswordKuat "Nama Admin"');process.exit(1)} const hash=await bcrypt.hash(password,12); await pool.query(`insert into users(username,full_name,role,password_hash,is_active) values($1,$2,'Master Administrator',$3,true) on conflict(username) do update set full_name=excluded.full_name,password_hash=excluded.password_hash,role='Master Administrator',is_active=true`,[username,name,hash]); console.log('Admin SOMBERE berhasil dibuat/direset:',username); await pool.end();})().catch(e=>{console.error(e);process.exit(1)});
